<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" xmlns:control="clr-namespace:VENDAS_SUPERMERCADO.Controls"
             x:Class="VENDAS_SUPERMERCADO.Views.FilterProductsView"
             BindingContext="{Binding Main, Source={StaticResource Locator}}">


   
    <StackLayout BackgroundColor="LightBlue">
        <Frame Margin="10,30,10,0" HeightRequest="500" BackgroundColor="LightGray" OutlineColor="Black" HasShadow="False">

            <StackLayout>

                <Frame Margin="-20,-20,-20,15" OutlineColor="Black" BackgroundColor="#708090" HeightRequest="30">
                    <Label Margin="0,-8,0,0" Text="Filtrar Produtos" TextColor="White" FontAttributes="Bold" FontSize="30" HorizontalOptions="Center"></Label>
                </Frame>
                
                <Frame Margin="-10,15,-10,15" OutlineColor="Black" BackgroundColor="LightGray" HeightRequest="10">
                    <control:BindablePicker Margin="0,-29,0,-28"  x:Name="pckDepartaments" TitleColor="Black" Title="Selecione o departamento:" 
                            ItemsSource="{Binding ListDepartaments}" 
                            SelectedItem="{Binding Departaments, Mode=TwoWay}" />
                </Frame>
                <Frame Margin="-10,0,-10,15" OutlineColor="Black" BackgroundColor="LightGray" HeightRequest="10" IsVisible="False">
                    <control:BindablePicker Margin="0,-29,0,-28" x:Name="pckOrders" TitleColor="Black" Title="Selecione a ordenação:" 
                            ItemsSource="{Binding ListOrderBy}" 
                            SelectedItem="{Binding OrderBy, Mode=TwoWay}"
                            IsVisible="False"/>
                </Frame>

                <Frame HasShadow="False" BackgroundColor="LightGray" Margin="-10,0,-10,0" HeightRequest="10">
                    <CheckBox x:Name="checkBoxOfertas" Color="Green" Margin="-20,-8,0,0" IsChecked="{Binding chkPromocao}"></CheckBox>
                </Frame>
                <Label Text="Produtos em Oferta" Margin="30,-47,0,0" TextColor="Black" FontSize="18"></Label>

                <Button Margin="0,60,0,0" BorderColor="Black" TextColor="White" BorderWidth="2" BackgroundColor="#708090" CornerRadius="9" x:Name="btnItem" Text="Confirmar" Command="{Binding FilterCommand}"/>
                
            </StackLayout>


        </Frame>
    </StackLayout>
    

</ContentPage>