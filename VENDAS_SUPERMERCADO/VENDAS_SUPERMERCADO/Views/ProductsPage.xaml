<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="VENDAS_SUPERMERCADO.Views.ProductsPage" Title="Produtos"
             BindingContext="{Binding Main, Source={StaticResource Locator}}">
    <StackLayout Margin="25">

        <SearchBar
             Placeholder="Buscar..."
             Text="{Binding ProductFilter, Mode=TwoWay}"
             SearchCommand="{Binding SearchProductCommand}">
        </SearchBar>
        <CollectionView
                HorizontalScrollBarVisibility="Never"
                HorizontalOptions="Start"
                ItemsSource="{Binding Products}">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Grid ColumnDefinitions="Auto" Padding="0,5,10,0">
                        <Frame Grid.Column="0" Grid.ColumnSpan="2" HorizontalOptions="FillAndExpand" HeightRequest="50" CornerRadius="20" Margin="2">
                            <Grid Grid.Row="1" RowDefinitions="Auto,Auto,Auto" ColumnDefinitions="Auto,*">
                                <Label Grid.Row="0" Grid.Column="0" Text="{Binding pro_nome}" FontSize="14" FontAttributes="Bold"/>
                                <Button Grid.Row="1" Grid.Column="0" Text="+" FontAttributes="Bold" BackgroundColor="#c12b0d" HorizontalOptions="Start" TextColor="White" WidthRequest="28" HeightRequest="28" FontSize="9" CornerRadius="5" Margin="0,10,0,0"/>
                                <Button Grid.Row="1" Grid.Column="0" Text="-" FontAttributes="Bold" BackgroundColor="#c12b0d" HorizontalOptions="Center"  TextColor="White" WidthRequest="28" HeightRequest="28" FontSize="9" CornerRadius="5" Margin="0,10,0,0" />
                                <Label Grid.Row="1" Grid.Column="0" Text="{Binding quantidade}" FontAttributes="Bold" VerticalOptions="End"  Padding="50,10,0,0"/>
                                <Label Grid.Row="1" Grid.Column="0" Text="{Binding Preco}" FontAttributes="Bold" VerticalOptions="Center" Padding="150,10,0,0"/>
                            </Grid>
                        </Frame>
                    </Grid>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
        <Button Text="TESTE"></Button>
        <Button Text="Meu Carrinho" HorizontalOptions="Center" VerticalOptions="EndAndExpand"></Button>
        <Picker x:Name="pckDPT" HorizontalOptions="FillAndExpand"
            Title="Departamento:"
            SelectedIndexChanged="pckDPT_SelectedIndexChanged">
            <Picker.Items>
                <x:String>ACOUGUE</x:String>
                <x:String>HORTI FRUTI</x:String>
                <x:String>BEBIDAS</x:String>
                <x:String>PADARIA</x:String>
                <x:String>FRIOS</x:String>
            </Picker.Items>
        </Picker>
        

    </StackLayout>
</ContentPage>